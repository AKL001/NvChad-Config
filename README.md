### Installation Instructions for NvChad

To install NvChad (UBUNTU), run the following command in your terminal:

```bash
git clone -b v2.0 https://github.com/NvChad/NvChad ~/.config/nvim --depth 1 && nvim
